services:
  edge-fallback:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: edge-fallback
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
